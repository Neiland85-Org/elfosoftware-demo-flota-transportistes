name: "CodeQL Security Analysis (Disabled)"

# 🚫 TEMPORALMENTE DESHABILITADO
# Motivo: Restricciones de organización requieren actions locales
# Para re-habilitar: Crear forks locales de las actions requeridas
# o contactar al administrador para permitir actions externas

on:
  workflow_dispatch: # Solo se puede ejecutar manualmente

jobs:
  disabled:
    name: "CodeQL Analysis (Disabled)"
    runs-on: ubuntu-latest
    steps:
    - name: Skip Analysis
      run: |
        echo "🚫 CodeQL analysis disabled due to organization action restrictions"
        echo "📝 To re-enable: Create local forks of required actions or allow external actions"
        echo "🔧 Required actions: actions/checkout, github/codeql-action"
        echo "📋 Current status: Waiting for organization policy update"
