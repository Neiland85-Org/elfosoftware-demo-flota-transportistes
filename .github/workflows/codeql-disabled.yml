name: CodeQL Analysis (Disabled)

on:  # Triggers: vacío para deshabilitar
  workflow_dispatch:  # Permite ejecución manual si es necesario

jobs:  # Jobs: vacío o con un job placeholder
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Skip CodeQL
        run: echo "CodeQL disabled for this workflow"